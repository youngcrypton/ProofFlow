[build]
builder = "NIXPACKS"
buildCommand = "npm run build -w @proofflow/domain && npm run build -w @proofflow/api"

[deploy]
startCommand = "npm run start -w @proofflow/api"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
