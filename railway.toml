[build]
builder = "NIXPACKS"
buildCommand = "npm run build:api"

[deploy]
startCommand = "bun run apps/api/src/server.ts"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
